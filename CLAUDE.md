# CLAUDE.md

## Development

- After merging to main, always run `go install .` to update the local binary.
- When making code changes, add or update tests as needed. All tests must pass (`go test ./...`) before considering work complete.
